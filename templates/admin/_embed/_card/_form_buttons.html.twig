<div class="d-flex justify-content-between align-items-center">
    <div>
        <a href="{{ listActionUrl }}" class="" style="text-decoration: none">
            <i class="fas fa-arrow-circle-left fa-lg" style="color: black"></i>
        </a>
        <a href="{{ modifyActionUrl }}"
           class="btn btn-default">
            <button type="submit" class="btn btn-primary">
                Save changes
            </button>
        </a>
    </div>
    {% if entity and entity.id %}
        <a class="btn btn-white" data-toggle="modal" data-target="#approveDeleteModal">
            <i class="fas fa-times fa-sm"></i> Delete row
        </a>
    {% endif %}
</div>